import { TrailingDotPipe } from './trailing-dot.pipe';

describe('TrailingDotPipe', () => {
  it('create an instance', () => {
    const pipe = new TrailingDotPipe();
    expect(pipe).toBeTruthy();
  });
});
